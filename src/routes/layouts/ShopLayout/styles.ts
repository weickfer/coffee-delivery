import styled from 'styled-components'

export const ShopLayoutContainer = styled.div`
  max-width: calc(68rem + 2rem + 1rem); // 40rem 28rem
  padding: 0 1rem;
  margin: 0 auto;

  > div {
    margin-top: 4rem;
  }

  /* > div[id='fix_on_top'] {
    position: fixed;
    top: 0;
    width: 100%;
  } */
`
