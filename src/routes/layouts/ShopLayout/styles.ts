import styled from 'styled-components'

export const ShopLayoutContainer = styled.div`
  > div {
    margin-top: 4rem;
  }

  /* > div[id='fix_on_top'] {
    position: fixed;
    top: 0;
    width: 100%;
  } */
`
